# ESLint complaining that can't use import need reuqire

# Make sure top-level package.json file has type set to commonjs
"eslintConfig": {
        "extends": [
            "react-app",
            "react-app/jest"
        ],
        "type": "commonjs"
    },



